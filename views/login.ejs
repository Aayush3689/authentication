<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>login</title>
    <style>
         *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        a{
            text-decoration: underline;
            font-weight: bold;

        }
        body, html{
            width: 100%;
            height: 100%;
            font-family: 'Courier New', Courier, monospace;
        }
        .container{
            min-height: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #0F192D;
            flex-direction: column;
        }
        form{
            max-width: 350px;
            padding: 1.8rem;
            background-color: #fff;
            border-radius: 10px;
        }
        form label, input{
            width: 100%;
        }
        form label{
            font-weight: 700;
        }
        form input{
            height: 2rem;
            margin-bottom: 1rem;
            outline: none;
            font-size: 1rem;
            padding-left: 0.8rem;
            border: 1px solid #00000080;
            border-radius: 5px;
        }
        form h1{
            text-align: center;
            margin-bottom: 1rem;
        }
        form button{
            display: block;
            padding: 0.5rem 1rem;
            border: none;
            font-size: 1rem;
            background-color: chocolate;
            color: #fff;
            border-radius: 5px;
            cursor: pointer;
            transition: all 0.5s ease;
            &:hover{
                background-color: rgb(227, 105, 18);
            }
        }
        .login_container{
            margin-top: 1rem;
            font-size: 1rem;
            p a{
                color: chocolate;
                transition: all 0.5s ease;
                &:hover{
                    color: #000;
                }
            }
        }
    </style>
</head>
<body>
    <div class="container">
    <form action="/login" method="post">
        <h1>Login</h1>
        <label for="useremail">Email</label>
        <input type="text" id="useremail" name="email" required>

        <label for="password">password</label>
        <input type="password" id="password" name="password" required>

        <button type="submit">submit</button>

        <div class="login_container"><p>don't have an account? <a href="/signup">signup</a> </p></div>
    </form>
</div>

<!-- alert -->
 <script>
    let userExist = (msg) =>{
        alert(msg);
    }
        if (isUserExist) {
            userExist(msg);
        }
 </script>
</body>
</html>